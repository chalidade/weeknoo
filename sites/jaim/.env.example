# Optional settings for the AI library (src/lib/ai). Copy to .env — it is read
# at build time, so anything here ends up inside the built site.

# Where Ollama listens. Only needed when it is not on the default port; a site
# with no .env at all already finds http://localhost:11434.
# VITE_OLLAMA_URL=http://localhost:11434
