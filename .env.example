# Copy to .env (git-ignored) and fill in.

# 21st.dev component registry — required to install components from @21st/*.
# Get a key at https://21st.dev/settings/api-keys
# `scripts/add-component.sh` loads this file, so one key here serves every site.
TWENTY_FIRST_API_KEY=
